{
  "name": "eleventy-starter",
  "version": "1.0.0",
  "private": true,
  "description": "Eleventy + Sveltia CMS starter for Push Pop.",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "deploy": "bash deploy.sh",
    "deploy:dry": "bash deploy.sh --dry-run"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
